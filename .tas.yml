framework: mocha
postMerge:
  pattern: 
    - "./test/**/*-test.js"
preMerge:
  pattern:
    - "./test/**/*-test.js"
preRun:
     command:
        - yarn install
postRun:
     command:
        - yarn --version
version: 1.0
