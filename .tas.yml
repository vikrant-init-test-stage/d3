framework: mocha
postMerge:
  pattern: 
    - "./test/d3-test.js"
preMerge:
  pattern:
    - "./test/d3-test.js"
preRun:
     command:
        - yarn install
postRun:
     command:
        - yarn --version
version: 1.0
