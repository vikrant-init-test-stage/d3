---
framework: mocha
preRun:
     command:
        - npm install
preMerge:
  pattern:
    - "./test/**/*-test.js"
postMerge:
  pattern: 
    - "./test/**/*-test.js"
postRun:
     command:
        - node --version
version: 1.0
